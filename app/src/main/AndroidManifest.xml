<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nearbuy">

    <application
        android:allowBackup="true"
        android:icon="@drawable/splash_screen_image"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NearBuy">
        <activity android:name=".VendorActivity"> </activity>
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".thanx"/>
        <activity android:name=".Vendor_order"/>
        <activity android:name=".CreateAccActivity"/>
        <activity android:name=".HomeActivity" />
        <activity android:name=".for_switch"/>
        <activity android:name=".MainActivity"> </activity>
    </application>

</manifest>